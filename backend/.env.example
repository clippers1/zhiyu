DATABASE_URI=postgresql://zhiyu:replace-with-a-strong-password@localhost:5432/zhiyu
PAYLOAD_SECRET=replace-with-at-least-32-random-characters
SERVER_URL=http://localhost:3108
COOKIE_SECURE=false
# Schema push is for a disposable development DB only. Production uses migrations.
PAYLOAD_DB_PUSH=false
ENABLE_DEMO_API=false
BOOTSTRAP_EMAIL=admin@example.com
BOOTSTRAP_PASSWORD=replace-with-a-unique-long-password
# Explicit opt-in: imports existing pending material as visibly unreviewed demo releases.
SEED_DEMO=false
