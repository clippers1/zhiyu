# Leave empty for offline/static demo data.
# Use /api/demo for the explicitly unreviewed beta collection, or /api/v1 for reviewed publications.
# Contract: GET /content and GET /content/:kind/:id. See docs/content-platform.md.
VITE_CONTENT_API_BASE_URL=
